import type {FC} from 'react';

import {CodeSample} from '@/lib';
import './index.css';

const App: FC = () => {
    return (
        <div>
            <CodeSample />
        </div>
    );
};

export default App;
